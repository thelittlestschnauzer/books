<h1> Your Book List: </h1>

<p> <% @books.each.with_index(1) do |book, index| %></p>

<div>
  <p>
    <%= index %>. <%= @book.title %> <%= book.description %>
  </p>
  <a href="/books/<%= book.id %>/edit">Edit this Book</a>
  <% end %>
</div>
